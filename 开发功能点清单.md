# QuickNote V2 开发功能点清单

## 1. 使用规则
- 状态标记：
  - `[x]` 已完成
  - `[~]` 进行中
  - `[ ]` 未开始
- 更新要求：
  - 每完成一个功能点，必须更新本清单。
  - 已完成项必须补一条“实现说明（简要）”。
  - 若有实现取舍，补一条“备注”说明原因。

## 2. 里程碑总览
- M0 工程与文档基线
- M1 核心笔记（UI + CRUD + 自动保存）
- M2 组织能力（标签/颜色/搜索）
- M3 悬浮交互（贴边/置顶/透明/收起）
- M4 提醒闭环（提醒/通知/Snooze）
- M5 增强能力（图片混排/语音转写/导入导出）

## 3. 功能清单（详细）

## M0 工程与文档基线
- [x] F-0001 文档体系初始化（需求、功能、架构、数据库）
  - 实现说明：新增 `功能点设计.md`、`架构设计.md`、`数据库设计.md`。
- [x] F-0002 运行入口初始化
  - 实现说明：`python main.py` 可启动应用。
- [x] F-0003 依赖清单初始化
  - 实现说明：新增 `requirements.txt` 并定义核心依赖。
- [x] F-0004 Git 仓库初始化与远程绑定
  - 实现说明：完成首轮提交并推送 `origin/main`。
- [x] F-0005 Git 规范与忽略规则
  - 实现说明：新增 `.gitignore` 并在 README 补充 Git 规范。

## M1 核心笔记（UI + CRUD + 自动保存）
- [x] F-1001 项目骨架搭建（`src/` 分层目录）
  - 实现说明：完成 `config/models/db/services/controllers/ui/utils` 目录与基础模块。
- [x] F-1002 主窗口与基础布局（顶栏/列表/编辑区）
  - 实现说明：完成主窗口分区、顶栏操作、列表区、编辑区及状态栏。
- [x] F-1003 SQLite 连接与 schema 初始化
  - 实现说明：实现连接管理、自动建表、外键启用。
- [x] F-1004 便签 CRUD（标题/正文）
  - 实现说明：完成新建、读取、编辑、软删除，走 Repo/Service/Controller 分层。
- [x] F-1005 自动保存（节流 800ms）
  - 实现说明：`QTimer` 单次计时，编辑停止 800ms 自动写库。
- [x] F-1006 基础删除与归档
  - 实现说明：实现软删除、归档/恢复按钮、归档视图切换。

## M2 组织能力（标签/颜色/搜索）
- [ ] F-2001 标签管理（增删改查）
- [ ] F-2002 标签绑定与过滤
- [x] F-2003 便签颜色切换
  - 实现说明：提供颜色下拉框，变更后落库并更新列表卡片背景色。
- [x] F-2004 Emoji 图标设置
  - 实现说明：提供 Emoji 下拉框，变更后落库并更新列表展示。
- [~] F-2005 搜索（标题 + 正文 + 标签）
  - 实现说明：已完成标题/正文检索；标签维度待补齐。

## M3 悬浮交互（贴边/置顶/透明/收起）
- [x] F-3001 Pin 置顶模式
  - 实现说明：通过 `WindowService + SettingsController` 切换 `WindowStaysOnTopHint`。
- [x] F-3002 透明度调节
  - 实现说明：滑杆控制窗口透明度（40%~100%）。
- [ ] F-3003 右侧贴边与悬停展开
- [ ] F-3004 收起模式（仅显示列表）
- [~] F-3005 低显性按钮交互优化
  - 实现说明：已加快捷键 `Ctrl+N / Ctrl+S / Delete`，后续继续收敛显性按钮。

## M4 提醒闭环（提醒/通知/Snooze）
- [ ] F-4001 提醒创建与编辑
- [ ] F-4002 到点调度（APScheduler）
- [ ] F-4003 系统通知
- [ ] F-4004 稍后提醒（Snooze）

## M5 增强能力（图片混排/语音转写/导入导出）
- [ ] F-5001 图片粘贴与混排显示
- [ ] F-5002 文本高亮
- [ ] F-5003 语音转写（硅基流动）
- [ ] F-5004 语音配置管理（API Key/Model）
- [ ] F-5005 Markdown 导出
- [ ] F-5006 Markdown 导入

## 4. 迭代记录
- 2026-03-19
  - 完成 M0 基线建设与远程仓库同步。
- 2026-03-19（骨架开发）
  - 完成 M1 主路径：骨架、主窗口、CRUD、自动保存。
- 2026-03-19（基本界面/交互/核心功能）
  - 补齐归档/恢复、颜色与图标切换、置顶与透明度控制、快捷键交互。
  - 完成核心可用链路：创建 -> 编辑 -> 自动保存 -> 搜索 -> 归档/恢复 -> 删除。

## 5. 下一步（执行中）
- [~] N-0003 开始 M2 标签系统（标签 CRUD + 便签标签绑定 + 标签筛选）。
