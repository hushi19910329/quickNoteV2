# QuickNote V2 开发功能点清单

## 1. 使用规则
- 状态标记：
  - `[x]` 已完成
  - `[~]` 进行中
  - `[ ]` 未开始
- 更新要求：
  - 每完成一个功能点，必须更新本清单。
  - 已完成项必须补一条“实现说明（简要）”。
  - 若有实现取舍，补一条“备注”说明原因。

## 2. 里程碑总览
- M0 工程与文档基线
- M1 核心笔记（UI + CRUD + 自动保存）
- M2 组织能力（标签/颜色/搜索）
- M3 悬浮交互（贴边/置顶/透明/收起）
- M4 提醒闭环（提醒/通知/Snooze）
- M5 增强能力（图片混排/语音转写/导入导出）

## 3. 功能清单（详细）

## M0 工程与文档基线
- [x] F-0001 文档体系初始化（需求、功能、架构、数据库）
  - 实现说明：新增 `功能点设计.md`、`架构设计.md`、`数据库设计.md`，作为后续开发蓝图。
- [x] F-0002 运行入口初始化
  - 实现说明：新增 `main.py`，确保项目可通过 `python main.py` 运行。
- [x] F-0003 依赖清单初始化
  - 实现说明：新增 `requirements.txt`，预置 PySide6、APScheduler、requests 等关键依赖。
- [x] F-0004 Git 仓库初始化与远程绑定
  - 实现说明：初始化 Git、创建 `main` 分支、提交基线并推送到 `origin/main`。
- [x] F-0005 Git 规范与忽略规则
  - 实现说明：新增 `.gitignore`，在 README 中补充分支策略和提交规范。

## M1 核心笔记（UI + CRUD + 自动保存）
- [ ] F-1001 项目骨架搭建（`src/` 分层目录）
  - 计划实现：按 `架构设计.md` 创建 `ui/services/db/models/controllers` 目录与基础模块。
- [ ] F-1002 主窗口与基础布局（顶栏/列表/编辑区）
  - 计划实现：使用 `QMainWindow + QWidget` 组合布局，按钮高度控制约 25px。
- [ ] F-1003 SQLite 连接与 schema 初始化
  - 计划实现：`db/connection.py + db/schema.py` 启动时自动建表。
- [ ] F-1004 便签 CRUD（标题/正文）
  - 计划实现：Repository 封装 SQL，Controller 驱动 UI 刷新。
- [ ] F-1005 自动保存（节流 800ms）
  - 计划实现：编辑变更信号 + `QTimer` 节流写库。
- [ ] F-1006 基础删除与归档
  - 计划实现：软删除标记 + 列表过滤。

## M2 组织能力（标签/颜色/搜索）
- [ ] F-2001 标签管理（增删改查）
  - 计划实现：`tags` + `note_tags` 表；侧栏标签面板。
- [ ] F-2002 标签绑定与过滤
  - 计划实现：便签编辑区选择标签，列表支持多标签组合过滤。
- [ ] F-2003 便签颜色切换
  - 计划实现：卡片色块选择器 + color 字段持久化。
- [ ] F-2004 Emoji 图标设置
  - 计划实现：内置 emoji 面板，保存到 `emoji_icon` 字段。
- [ ] F-2005 搜索（标题 + 正文 + 标签）
  - 计划实现：首期 `LIKE` 查询，后续可升级 FTS5。

## M3 悬浮交互（贴边/置顶/透明/收起）
- [ ] F-3001 Pin 置顶模式
  - 计划实现：窗口 flag 切换并持久化设置。
- [ ] F-3002 透明度调节
  - 计划实现：`setWindowOpacity` + 设置持久化。
- [ ] F-3003 右侧贴边与悬停展开
  - 计划实现：监听窗口位置和鼠标进入离开事件，执行吸附动画。
- [ ] F-3004 收起模式（仅显示列表）
  - 计划实现：编辑区折叠，列表区全宽展示。
- [ ] F-3005 低显性按钮交互优化
  - 计划实现：常用动作迁移到右键菜单、悬停工具条和快捷键。

## M4 提醒闭环（提醒/通知/Snooze）
- [ ] F-4001 提醒创建与编辑
  - 计划实现：`reminders` 表 + 提醒设置弹窗。
- [ ] F-4002 到点调度（APScheduler）
  - 计划实现：启动加载提醒并注册 job，触发后更新状态。
- [ ] F-4003 系统通知
  - 计划实现：`plyer.notification` 触发本地通知。
- [ ] F-4004 稍后提醒（Snooze）
  - 计划实现：提供 5/15/60 分钟快捷按钮，更新下次提醒时间。

## M5 增强能力（图片混排/语音转写/导入导出）
- [ ] F-5001 图片粘贴与混排显示
  - 计划实现：编辑器拦截粘贴事件，保存图片文件并插入 HTML 引用。
- [ ] F-5002 文本高亮
  - 计划实现：基于 `QTextCharFormat` 应用背景色。
- [ ] F-5003 语音转写（硅基流动）
  - 计划实现：录音文件上传 API，返回文本插入当前便签。
- [ ] F-5004 语音配置管理（API Key/Model）
  - 计划实现：设置页配置 + `settings` 表持久化。
- [ ] F-5005 Markdown 导出
  - 计划实现：便签转 Markdown，图片资源导出到同目录。
- [ ] F-5006 Markdown 导入
  - 计划实现：解析 Markdown 并重建便签、标签和附件关联。

## 4. 迭代记录
- 2026-03-19
  - 完成 M0 全部项（F-0001 ~ F-0005）。
  - 已建立远程仓库并完成基线提交。

## 5. 下一步（执行中）
- [~] N-0001 开始 M1：搭建 `src/` 分层目录与最小主窗口骨架。
