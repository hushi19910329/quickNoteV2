# QuickNote V2 开发功能点清单

## 1. 使用规则
- 状态标记：
  - `[x]` 已完成
  - `[~]` 进行中
  - `[ ]` 未开始
- 更新要求：
  - 每完成一个功能点，必须更新本清单。
  - 已完成项必须补一条“实现说明（简要）”。
  - 若有实现取舍，补一条“备注”说明原因。

## 2. 里程碑总览
- M0 工程与文档基线
- M1 核心笔记（UI + CRUD + 自动保存）
- M2 组织能力（标签/颜色/搜索）
- M3 悬浮交互（贴边/置顶/透明/收起）
- M4 提醒闭环（提醒/通知/Snooze）
- M5 增强能力（图片混排/语音转写/导入导出）

## 3. 功能清单（详细）

## M0 工程与文档基线
- [x] F-0001 文档体系初始化（需求、功能、架构、数据库）
  - 实现说明：新增 `功能点设计.md`、`架构设计.md`、`数据库设计.md`，作为后续开发蓝图。
- [x] F-0002 运行入口初始化
  - 实现说明：新增 `main.py`，确保项目可通过 `python main.py` 运行。
- [x] F-0003 依赖清单初始化
  - 实现说明：新增 `requirements.txt`，预置 PySide6、APScheduler、requests 等关键依赖。
- [x] F-0004 Git 仓库初始化与远程绑定
  - 实现说明：初始化 Git、创建 `main` 分支、提交基线并推送到 `origin/main`。
- [x] F-0005 Git 规范与忽略规则
  - 实现说明：新增 `.gitignore`，在 README 中补充分支策略和提交规范。

## M1 核心笔记（UI + CRUD + 自动保存）
- [x] F-1001 项目骨架搭建（`src/` 分层目录）
  - 实现说明：按架构创建 `config/models/db/services/controllers/ui` 分层和基础模块文件。
- [x] F-1002 主窗口与基础布局（顶栏/列表/编辑区）
  - 实现说明：实现 `MainWindow`，包含顶栏操作区、便签列表区、编辑区，按钮高度为 25px。
- [x] F-1003 SQLite 连接与 schema 初始化
  - 实现说明：实现 `DatabaseConnection` 与 `init_schema`，启动时自动建表并开启外键约束。
- [x] F-1004 便签 CRUD（标题/正文）
  - 实现说明：实现 `NoteRepository + NoteService + NoteController`，完成新建、读取、编辑、软删除。
- [x] F-1005 自动保存（节流 800ms）
  - 实现说明：编辑器变更触发 `QTimer` 单次计时，800ms 后写库，避免频繁 I/O。
- [~] F-1006 基础删除与归档
  - 实现说明：已完成软删除；归档入口和归档视图过滤待补充。
  - 备注：本轮先保证最小骨架可用，归档在下一步补齐。

## M2 组织能力（标签/颜色/搜索）
- [ ] F-2001 标签管理（增删改查）
- [ ] F-2002 标签绑定与过滤
- [ ] F-2003 便签颜色切换
- [ ] F-2004 Emoji 图标设置
- [~] F-2005 搜索（标题 + 正文 + 标签）
  - 实现说明：已完成标题/正文内存搜索骨架；标签维度过滤待接数据库层。

## M3 悬浮交互（贴边/置顶/透明/收起）
- [ ] F-3001 Pin 置顶模式
- [ ] F-3002 透明度调节
- [ ] F-3003 右侧贴边与悬停展开
- [ ] F-3004 收起模式（仅显示列表）
- [ ] F-3005 低显性按钮交互优化

## M4 提醒闭环（提醒/通知/Snooze）
- [ ] F-4001 提醒创建与编辑
- [ ] F-4002 到点调度（APScheduler）
- [ ] F-4003 系统通知
- [ ] F-4004 稍后提醒（Snooze）

## M5 增强能力（图片混排/语音转写/导入导出）
- [ ] F-5001 图片粘贴与混排显示
- [ ] F-5002 文本高亮
- [ ] F-5003 语音转写（硅基流动）
- [ ] F-5004 语音配置管理（API Key/Model）
- [ ] F-5005 Markdown 导出
- [ ] F-5006 Markdown 导入

## 4. 迭代记录
- 2026-03-19
  - 完成 M0 全部项（F-0001 ~ F-0005）。
  - 已建立远程仓库并完成基线提交。
- 2026-03-19（骨架开发）
  - 完成 M1 的骨架主路径：F-1001 ~ F-1005。
  - F-1006（归档）与 M2+ 功能将在后续迭代完成。

## 5. 下一步（执行中）
- [~] N-0002 完成 F-1006 的归档流程（归档按钮、数据库字段切换、列表过滤）。
